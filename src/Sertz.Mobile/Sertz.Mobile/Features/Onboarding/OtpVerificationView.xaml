<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Sertz.Mobile.Features.Onboarding"
             x:Class="Sertz.Mobile.Features.Onboarding.OtpVerificationView"
             BindingContext="{vm:OtpVerificationViewModel}"
             x:DataType="{vm:OtpVerificationViewModel}">
    
    <VerticalStackLayout Spacing="25" Padding="30" VerticalOptions="Center">

        <Label Text="OTP Verification"
               FontSize="28"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <Label Text="{Binding VerificationMessage}"
               HorizontalOptions="Center"
               TextColor="Gray"
               FontSize="16" />

        <Border Stroke="LightGray"
                StrokeThickness="1"
                Padding="0"
                Margin="0, 10, 0, 10">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>

            <Entry Placeholder="Enter 6-digit OTP"
                   Keyboard="Numeric"
                   MaxLength="6"
                   HorizontalTextAlignment="Center"
                   Margin="15,0"
                   FontSize="22"
                   Text="{Binding OtpCode, Mode=TwoWay}" />
        </Border>

        <Button Text="Verify and Proceed"
                TextColor="White"
                BackgroundColor="#FF5722"
                CornerRadius="8"
                HeightRequest="50"
                Command="{Binding VerifyOtpCommand}" />

        <HorizontalStackLayout HorizontalOptions="Center" Margin="0, 10, 0, 0">
            <Label Text="Didn't receive code?" TextColor="Gray" />
            <Button Text="Resend" 
                    StyleClass="TextButton" 
                    TextColor="#007ACC" 
                    Margin="10 0 0 0"
                    Command="{Binding ResendOtpCommand}" />
        </HorizontalStackLayout>
        
    </VerticalStackLayout>
</ContentPage>